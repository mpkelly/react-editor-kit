import { Range, Node, NodeEntry } from "slate";
export declare const highlightCode: ([node, path]: NodeEntry<Node>, language: string) => Range[];
export declare const getContent: (token: any) => any;
