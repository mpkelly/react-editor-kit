import { Element } from "slate";
export declare const useTables: () => {
    addColumn: (element: Element) => void;
    addRow: (element: Element) => void;
    deleteColumn: (element: Element) => void;
    deleteRow: (element: Element) => void;
};
